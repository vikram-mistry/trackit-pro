# Trackit Pro

Personal daily expense and utility tracker optimized for iPhone Safari.

## Setup
1. `npm install`
2. `npm run dev -- --host`

## Installation on iPhone
Open the network URL in Safari and select "Add to Home Screen".
